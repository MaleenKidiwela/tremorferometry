# 2026-07-19 Notes — broadband fleet monitoring (83→104/178)

Master plan / resume: `notes/FLEET_BROADBAND_PIPELINE.md` (§1 RESUME, §2 LIVE STATE, §4 taxonomy, §7 changelog).
Batch is the detached `fleet_broadband_all.sh` + `fleet_download_ahead.sh` + `fleet_trace_janitor.sh` trio;
STEP ZERO after any crash = process check before touching anything.

## Progress
- 104/178 processed · INCLUDE 69 (+3 anchors) · FLAG 34 · include rate ~66%. ~6000+ certified families banked.
- Batch healthy throughout; all three processes alive (main up ~3d, janitor relaunched 07-18).
- Top INCLUDEs: OTR 155, WISH 153, MARQ 141, JEDS 137, TXDB 127, RADR 119, LBR 119, TOLE 117, MGRB 117, SNB 115.

## Lessons / decisions settled while monitoring (all now in FLEET_BROADBAND_PIPELINE §4 + §7)
1. **CODA-LESS FLAG sub-type (new, confirmed 2×).** LRIV 0/128 and PR01 0/300 both wipe out despite a clean
   full pipeline. Diagnostic = the causality-ratio distribution has NO high tail: `max(causality) < 1.4`
   across ALL families (LRIV 1.35, PR01 1.32) vs real stations reaching 3-13 (OTR 13.2). These are
   emergent/microseism-dominated sites (LRIV = N. Olympic coast/Strait; PR01 = CC volcano near Rainier) with
   repeating clustered signal but no impulsive coda. Correctly rejected — NOT a bug.
   - Self-correction: I first guessed "noise-carrier = too many detections." WRONG — KSXB (INCLUDE 78) has
     15-23M det/yr, ~10× LRIV's 1-2.7M. Detection count and family count do NOT discriminate; only the
     causality-TAIL shape does.
2. **CN is not a systematic NRCan case.** Earlier §4 note overstated it. Most CN is well-served at IRIS
   (TOFB/CBB/SNB/MGRB/TXDB strong INCLUDE); only individual sparse stations (MYRA 161 days, SHPB 48 day-files)
   need NRCan. Banked → `data/cn_sparse_nrcan.txt`, end-of-run retry. Don't wire NRCan mid-batch for 2 stations.
3. **Near-miss category noticed:** TRIPT 17 certified @ 20% survival = count-short but survival-fine (mirror of
   tier-2, which is count-OK/survival-short). Family-supply-limited (84 clustered). If more accumulate → list.

## FLAG ledger this stretch (all correctly gated, none a bug)
- CODA-LESS: LRIV, PR01
- CN sparse-at-IRIS: SHPB (+ MYRA earlier)
- near-miss count-short: TRIPT (17/20 @20%)
- genuine-weak <50 families: PCOR 7, WEAV 16, DOSE 18, TKEY 17, PF27 49; low-survival: UCHR 13 @10%, CINE 12

## Pending (end-of-run)
- Drain re-run queue `data/rerun_queue.txt` (6: FISH, JCC, GOBB, SYMB, KMR, KHMB).
- NRCan retry for `data/cn_sparse_nrcan.txt` (MYRA, SHPB).
- Tier-2 + near-miss inversion-inclusion decision (evidence-based).
- Then: 4-D δβ/β inversion (boreholes + broadband).
