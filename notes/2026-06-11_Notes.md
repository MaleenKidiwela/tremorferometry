# 2026-06-11 Notes — Family Trust Test execution (Phase A)

## 1. B018 CERTIFICATION COMPLETE (4/4 Tier-1 anchors pass)
- Day/night split-stack dv/v (b018_daynight_test.sh): **2016-17 drop in BOTH independent arms** (day-only
  −0.91%, night-only −0.75%), corr +0.62 over 6923 days (half-data attenuation expected) → MEDIUM-carried.
- With stack-vs-random (10.8×/3.2×), ACF slope 0.34, coherent non-ACF drops → **B018 dv/v = REAL** (06-10 §18).

## 2. Tier-0 margin sweep DONE (scripts/family_trust_tier0.py → data/family_trust_provisional.csv)
2458 families: 1038 CLEANISH / 781 SUSPECT / rest MIXED. Highlights:
- **B935 best in network**: 109/113 cleanish, tremor-corr 0.26 (by far highest — families fire WITH tremor).
- Healthy majority: B201 48/49, B017 65/77, B045 66/100, B927, B928, B932, B036, B014, B030, NLLB, COLT.
- KNOWN FLAG ARTIFACTS (read with care): B020/B028 spiky-flag over-fires (their genuine templates are
  high-frequency); PGC/HDW low coda-cc is station character (PGC detection stats pristine); **B018 itself
  shows 25 "suspect" yet is Tier-1-certified real — flags ≠ verdicts, again.**
- Tier-1 priority: COR (dn 2.0), B033 (dn 2.5), B005, cap-saturated B039/B001/B941/B022/B023/B932, B003.

## 3. Reversed-template null (T1c) — the false-rate measurement
Time-reversed B018 templates (data/b018_reversed_templates.npz) through the SAME detector, 2015-2016:
**1.66M detections in 2015 alone ≈ 80/family/day = same rate as the real templates.** → B018's detection
LISTS are mostly noise-matches (false-rate ~comparable to forward rate) while the STACKS carry the real
signal — both true at once; the trust battery exists precisely for this regime.

## 4. In flight
- scripts/family_trust_tier1.py on B018 (chained after reversed 2016): T1a stack-vs-random coda-σ per family
  + odd/even split-cc, calibrated against the reversed fakes. VALIDATION GATE: real families must separate
  from reversed nulls. → data/family_trust_tier1_B018.csv, logs/tier1_B018.log.
- Cleaned 87 day-old orphan multiprocessing workers (mem 24→13G).
Master plan: notes/MASTER_PLAN_2026-06-11.md (handoff-ready). Design: notes/FAMILY_TRUST_TEST.md.

## 5. Tier-1 v2 VALIDATED (after Fable adversarial review caught glitch-window bug)
Review verdict on v1: NO-GO — 6 "real" families had coda_sigma up to 6485 = DATA-GAP GLITCH windows
(zero-fill -> tiny S-norm -> exploded window dominates stack); would have been crowned most-trusted (σ feeds
inversion weights). Plus nwin-mismatched null, nboot=12 wobble, half-split shared-component trap.
v2 amendments applied (scripts/family_trust_tier1.py): gap/amplitude window QC + 2% trimmed stack,
nwin-matched nulls, nboot=200, per-window hour -> TRUE day/night T1b, tri-state verdicts.
**v2 B018: real median 18.0σ (max 40.8) vs fakes 2.2σ (95th 8.2); daynight_cc 0.68; 44 TRUSTED / 9
UNDETERMINED / 3 FAIL (incl c276). GATE PASSED.** -> data/family_trust_tier1_B018.csv
ROLLOUT (review order): B003 (open question + negative control; re-download RUNNING, janitor PAUSED) ->
B935 (positive control) -> COR/B033/B005/cap-saturated set.
LESSONS: inspect your own output's extremes before reporting; never pkill a pattern your own shell echoes
(bracket trick — bitten AGAIN).

## 6. WHAT ARE THE NON-LFE FAMILIES? (Fable taxonomy + classifier launched)
Fable advisory (read-only; full result in transcript). KEY REFRAME: class (a) is NOT a family class — it's a
DETECTION-LIST noise-fraction CONTINUUM; the TEMPLATES are often genuine LFEs (provisional-CSV: diurnal-group
template shape == genuine, so shape carries no (a)-vs-real info). Real repeats ~1-5/day drowned under ~95/day
false matches. LABEL = DROWNED-LIST, rescue-pending (Phase-C 3-comp/adaptive detector can recover them). Do NOT
fossilize as "shallow noise families."
PHYSICAL IDENTITIES:
- Class (a) carrier = CULTURAL/anthropogenic noise (B018 verified: weekday 1.31, work-hours 0.56 vs 0.375 random).
  Ocean microseism RULED OUT (0.1-0.5 Hz, below 2-8 Hz band). Wind/hydro secondary (diurnal, week-flat).
- Class (b) spiky = NATURAL SEISMICITY (B933: week-flat 0.98, NIGHT-favored = real-EQ detectability signature;
  Mendocino TJ). Sub-split: coda-STABLE = repeating-EQ multiplets (legit crustal CWI, wrong depth) vs
  coda-COLLAPSED = glitch/pileup (exclude, already cc-gated out).
USABLE SHALLOW PRODUCT = the WHITENED template-free ACF (scripts/autocorr_dvv.py, whitening now ADDED), NOT family
stacks (worse composition control). FLAW-8: site-carrier + whitened ACF = 2 independent site-term constraints, BUT
must whiten first (else inject anti-correlated fake fault signal) + calibrate coupling per station (not =1).
RECHECK flagged: "(b) day-skewed best detections" — B933 full list is NIGHT-favored/week-flat; the day-skew may be
a censored-population artifact. CLASSIFIER LAUNCHED: scripts/family_fingerprint.py -> data/family_fingerprint.csv
(weekly×hourly per family: CULTURAL/BLAST/NATURAL-like/DIURNAL-other/FLAT-mixed). Next: B933 vs B045/B932/B049
cross-station coincidence (free, on disk) to confirm natural-EQ identity of the spiky class.

## 7. ORIGIN OF THE CONTAMINATION (user memory of an early rate discriminator — confirmed)
User recalled an early discriminator removing families with too many repeats/day. Traced it: METHODS.md E.7j.
The ORIGINAL PGC/V.I. discovery had TWO protections the 35-station margin pipeline LOST:
  1. 2-STATION network CC (all-pairs across PGC+LZB) — families had to cohere at BOTH stations = built-in
     cross-station coincidence that rejects single-station noise.
  2. Explicit exclusion of the >200-member transitively-merged super-cluster (the over-detecting blob).
The scaled-up GPU pipeline (scripts/discover_gpu.py) is SINGLE-STATION, complete-linkage, cc>=0.8,
min-members>=3 — kept complete-linkage (no over-bridge) but DROPPED the network requirement AND any rate/size
cap. => contamination crept in exactly at the 2-station -> 1-station scaling for the margin array.
IMPLICATION: this VALIDATES Phase C (rebuild detector w/ network coincidence <50km) — it literally re-adds the
early protection at scale. The trust battery (reversed-template + cross-station) reconstructs post-hoc what
2-station discovery enforced up front.
CAUTION: do NOT hard-drop high-rate families now (Fable drowned-list: their TEMPLATES are real LFEs, rescuable
by the rebuilt detector). Cap-saturation is already a Tier-0 flag; the battery + detector rebuild handle it.

## 8. B003 trust verdict + cross-station source-recovery (honest negatives)
B003 Tier-1 (full battery, re-downloaded): 81 fams -> 17 TRUSTED / 24 UNDETERMINED / 40 FAIL. BUT real-vs-fake
SEPARATION COLLAPSED: real coda median 0.5σ == fakes 0.5σ (vs B018 18 vs 2.2). => B003 originals are largely
INDISTINGUISHABLE FROM REVERSED FAKES; the 17 "TRUSTED" are a thin tail, not a clean population. B003 = mostly
not-real, as expected (off-band). Battery worked as a negative control. Master table updated; waveforms cleaned.
CROSS-STATION SOURCE-RECOVERY (B018 c615 drowned-list vs CPW 9km): INCONCLUSIVE. Zero-lag coincidence 1.0x null;
lag-histogram peak only 1.3-1.5x even for the TRUSTED positive control (and at the +29s window edge = artifact).
WHY: both B018 & CPW lists are ~90/day noise-dominated -> genuine few/day events buried on BOTH sides; coincidence
needs the genuine (high-cc) subset isolated FIRST. CONCLUSION: cross-station source-ID is a PHASE-C capability
(network coincidence AT detection time finds genuine events as coincident from the start) — NOT a retrofit on raw
drowned lists. Near-term drowned-source characterization = stack top-cc subset / discovery-seed windows (~Tier-1).

## 9. KEY REFINEMENT — station-level separation gate (from B003)
Per-family TRUSTED labels are ONLY meaningful when the station shows real-vs-fake SEPARATION (real coda-σ median
>> reversed-fake median). B018: 18 vs 2.2 -> trust labels. B003: 0.5 vs 0.5 -> NO separation -> the 17 "TRUSTED"
are likely the fake tail, NOT real -> QUARANTINE the whole station, don't keep 17. TODO: add a station-separation
metric to family_trust_tier1.py output (e.g. real_median/fake_median, or KS distance) and a station verdict
{RELIABLE / QUARANTINE}. Rollout interpretation: a station only contributes certified families if it PASSES the
separation gate first.
MASTER TABLE: B018 RELIABLE (44/9/3), B003 QUARANTINE (no separation). Rollout next: B935 (positive control).

## 10. cc-THRESHOLD does NOT purify (tested, 3 stations) + contaminant-source map
Raising cc makes contamination WORSE: day/night ratio vs cc -> B018 0.99->1.39(cc.92); B935 0.92->3.14(cc.97);
B033 0.89->2.29(cc.95). High-cc tail = MOST contaminated. Mechanism: real LFEs emergent (modest cc); contaminants
(blasts/cultural/EQ) sharp (high cc) -> high-cc cut selects AGAINST LFEs, FOR contaminants (same as SNR trap).
[caveat: absolute dn here PST-frame vs earlier UTC; the TREND is the robust result.] => NO cc threshold fixes the
current 2-s/Z detector; the fix is N_eff (3-comp + longer template) + network coincidence (Phase C).
FINGERPRINT (data/family_fingerprint.csv, 2527 fams): per-station contaminant SOURCE identified -
ANTHROPOGENIC (cultural+blast, weekday/midday): B018, B033, CPW, GNW, COLT, B004. NATURAL-seismicity (week-flat/
night): B935(108), B017, B022, B001, B201, COR, B040... AMBIENT/flat: B003, B020, B045, PGC, B013.
B018 blast/cultural CONFIRMED anthropogenic (weekend 0.21 vs 0.29 random, midday 0.38 vs 0.25, peak 8-11am;
no holiday dip -> general industrial not pure quarry). All tests run on ALREADY-DENSIFIED families (lists/stacks
on disk); only waveforms re-downloaded for stack tests; NO re-densify until Phase-C rescue.

## 11. CORRECTION to §8/§9 — B003 has ~16 REAL families (do NOT quarantine the station)
My "B003 QUARANTINE, no separation" was WRONG (judged by MEDIAN). Correct: B003 fakes max 6.6sigma; 16 real
families exceed it (up to 34.9sigma, 13 above 10sigma, day/night cc 0.77-0.91), sources at 47.8-48.95N
(Olympic/S-VI). So ~16 of 81 B003 families are GENUINELY real; the bulk (65) is noise. "Some of B003 are real"
is correct (user was right).
FIX TO THE STATION GATE: keep families ABOVE THE FAKE MAX (tail test), NOT "require median real > median fake".
A small real population under a noisy bulk (16 real/65 noise) is wrongly quarantined by a median test. Apply the
tail-above-fake-max rule per station. B003 verdict -> KEEP 16 real, drop 65. B018 -> 44 (unchanged, it separates
at both median and tail). Master-table interpretation must use the tail rule, not the median separation gate.

## 12. CERTIFIED PRODUCTS + rollout launched
Saved certified per-station dv/v (REAL families, coda_sigma>fake_max): data/daily_dvv_B018_2to4_CERTIFIED.csv
(40 fams), data/daily_dvv_B003_2to4_CERTIFIED.csv (16 fams). Split-plot figures/B018_B003_real_vs_noise_dvv.png:
REAL std 0.13%(B018)/0.17%(B003) vs NOISE 0.24%/0.45% -> separation works, B003's 16 real ARE usable.
Deleted B018/B003 waveforms (re-downloadable). ROLLOUT QUEUE launched (scripts/rollout_queue.sh):
B935(pos control)->B017->B201->B022->B001->B928->B014->B040, each full-download->run_trust_battery(auto-cleans
waveforms)->next, sequential. Verdicts -> data/family_trust_master.csv. Gate rule for interpretation:
keep families with coda_sigma > station's fake-max (tail rule, NOT median).

## 13. ROLLOUT COMPLETE + detection-cleaning (circular) + T2a site-filter (Fable-audited)
**Rollout done (10 stations).** Two-gate scoreboard, GOLD=coda_sigma>station fake-MAX (strict tail), TRUSTED=>fake-95:
341 GOLD / 433 TRUSTED / 114 FAIL over 781 scored families. Rich tier B022(67)/B040(62)/B001(60)/B018(40)/B201(23),
solid B928(33)/B014(19), marginal B003(16)/B017(15)/B935(6). Elevated-floor caveat (fake reaches 14-16sigma):
B201/B022/B014 have a coherent persistent site signal -> per-station reversed calibration auto-raises their bar
(WHY we calibrate per-station). Saved data/family_trust_rollout + memory trust-battery-rollout-2026-06-11.

**Detection-level cleaning experiment = CIRCULAR, discarded.** Kept detections whose 2-4s coda matched the family
ALL-TIME-MEAN coda (>0.20), restacked, re-measured dv/v. But dvv_roll30cal's ref IS the all-time mean -> we
discard the deviation we then measure. Scatter+seasonal fell for ALL families, MOST for FAIL (the circularity
control firing). cc_max->0.98 = readout of the selection, not quality. Fable confirmed. Scripts exp_b935_detclean_*
kept as cautionary record; results NOT for use. Probe (exp_b935_detclean_probe.py) sound: GOLD detections coda-CC
+0.18 over random, FAIL +0.05.

**T2a = SITE filter, NOT depth cert (key reframe, Fable).** It measures whether family dv/v TIME-SERIES tracks the
shallow Z-ACF dv/v = contamination filter; it does NOT certify deep KERNEL (geometry) or deep CHANGE (temporal).
B935 T2a uninformative (ACF monitor dead, cc median 0.39). B018 (cc 0.94) T2a-FIXED with 30-day BLOCK-BOOTSTRAP CIs
(scripts/exp_t2a_fixed.py): of 39 GOLD, 21 SHALLOW-COUPLED (candidate site-carriers), 15 DECOUPLED (deep-or-noise),
3 ANOMALOUS (neg slope, model-violating). 8 confident site-carriers (R2>=0.4) incl high-sigma c554/c891/c1036/c285.
**CRITICAL: T1 sigma is ORTHOGONAL to shallow-coupling (Spearman -0.08, p=0.61; shallow-coupled median sigma 22.7 ~
decoupled 23.6) -> CANNOT sigma-weight out site-carriers; inversion input MUST be T2a-filtered.**
Bugs Fable caught in 1st T2a run (fixed): CIs were on raw not deseasoned slope; ~5x too tight (n=3600 daily but
30-day-rolling ~120 indep); ~1/3 GOLD had impossible negative slopes.

**Honest depth ceiling reaffirmed:** deep velocity CHANGE = bound ~0.02% (cannot positively confirm; inter-station
coherence is confounded by regional shallow climate AND already done = 505-pair shared-source null, excess -0.006).
Deep SENSITIVITY (geometry) positively confirmable via dt-vs-lapse DILATION + multi-window kernel -> needs `_calS`
per-window true-scale regen (pending; `_calT` still ~1.5:1 inter-window inconsistent).

**Open (await user steer):** (2) `_calS` regen -> lapse-dilation deep-sensitivity test, vs (3) consolidate reframe
into MASTER_PLAN first. Disk: 13GB leftovers ERW/SNB/LRIV/LZB + B935 41GB (poor pilot, dead ambient) pending OK to clear.
