# 2026-07-10 — Borehole pipeline rebuild + FATAL densify truncation bug (Merlin audit)

## ★ FATAL BUG FOUND (Merlin audit) — densify truncated 100 Hz days to 9.6 h
`scripts/densify_gnw_gpu.py` had `N_FIX = 3_456_016` HARDCODED for 40 Hz (40×86400=3,456,000).
At fs=100 a day is 8,640,000 samples; line ~198 `data[:N_FIX]` silently kept only the first
**9.6 hours (00:00–09:36 UTC)** of every day → ~60% of data discarded, from a fixed diurnal slice.
- Verified empirically: hour-of-day of mf_b011p90_2012.csv = hours 0–9 only.
- Affected EVERY this-session densify at 100 Hz (mf_b011p90*, mf_b926p90*) AND the older
  picker-thread runs (b011p50/p70 — same m=200). The old 40 Hz margin fleet was UNAFFECTED
  (3,456,000 = exactly a 40 Hz day).
- All this-session detection stats were on truncated data (the "~190 det/family/day", "~400M
  total", cap on/off comparison — all wrong, ~2.5× low).

## THE FIX — process at 40 Hz (Option B, Merlin's firm rec over segmentation / 100 Hz)
Decision (user + Merlin): **densify + stacking + dv/v at 40 Hz; discovery + picker + fingerprint
stay at 100 Hz.** Why 40 Hz:
- N_FIX = a 40 Hz day → correct by construction, no truncation, no GPU OOM, no new GPU code.
- **Fleet-consistent**: the 28-station fleet dv/v, B018 verification, step-injection benchmarks
  were ALL at 40 Hz — the inversion assembles B011/B926 with them.
- 2.5× less compute/storage. dv/v is rate-independent (stretch), so values still compare.
- Fingerprint stays 100 Hz (picker trained at 100 Hz; HF features hf_ratio/bf_16up/sp_slope/
  centroid depend on >20 Hz content → a full 40 Hz switch would need picker retraining. AVOIDED.)
Implementation: resample discovery template npz 100→40 (resample_poly 2/5, m 200→80) ONCE per
station, keep summary/members; densify `--fs 40`, NEW prefix `mf_<sta>p90f40_`, regen ALL years
(never mix rates — resume-skip would splice truncated 100 Hz years with 40 Hz years). Pass `--fs 40`
to build_long_window_3comp.py too (default 100). Code edits made: N_FIX = int(round(fs))*86400+16
(fs-aware); template-batching (TB=128) in the FFT loop so 530-template B926 stays ~28 GB GPU.
VERIFIED: B011 40Hz 2010 = 86.6M rows in 7.23 min, hours 0–23 ✓, GPU 24.5 GB.

## OTHER Merlin audit findings (STILL OPEN — do before any dv/v)
- **Flaw 3 (MAJOR): cap-off detection stream is noise-dominated + WINTER-modulated** (rate peaks
  Dec–Mar, bottoms in Sept = ETS month; tracks microseism, anticorrelated w/ tremor). Off-episode
  daily stacks = template-matched noise; seasonal noise fraction can MANUFACTURE a seasonal dv/v.
  → MUST run reversed-template fake-rate control λ(t) + cc≥0.9 monthly-histogram test BEFORE
  stacking; gate days/families on rate ≫ fake rate.
- **Flaw 2 (FATAL as validation): the Lin-coincidence "99% confirmed" was HOLLOW** — candidates are
  tremor-window-only + Lin is dense (882k/80km, gaps 0.02 s) → coinc≈1 by construction; 37-day null
  trivially lands off-episode → 0. Certifies nothing. Replace with P1.2 **B011↔PGC cross-instrument
  coincidence (±2 s)** — co-located, different networks, still not run.
- **Flaw 5: family gate uses classifier-P-on-stacks** — the 2026-07-03 plan declared this
  FATAL-circular and CUT it (demote picker to seed-filter thr~0.2–0.3). And **trust battery (stage 8)
  must run BEFORE dv/v (stage 9).** Current 456/530 are candidate lists, not certified input.
- Flaw 6: dvv_roll30cal.py `--origin-anchor` must be ON (t_s=+1.0 s) or 2–4 s dv/v diluted ~0.44×;
  script doesn't log anchor state — verify + fix stale help text.

## PIPELINE STATE (borehole-first, 3-lane conveyor)
- Certified families (100 Hz discovery, VALID — unaffected by the densify bug): B011 456 (P>0.9),
  B926 530 (LFE-label; cross-station B011 picker under-scores P, so use label not P>0.9).
- GPU: B011 densify @40Hz running (~2 h). Then B926 @40Hz.
- CPU: 3-ch stacker `build_long_window_3comp.py` (NEW, per-year streaming for memory) ready `--fs 40`.
- NET: download queue B004→B001→B003→B005→B013→B014 (all PB boreholes, Lin region).
- ⚠ pod memory cap = **187 GB** (cgroup; `free` lies, shows 1.5 TB). Watch anon, not total (cache).
- ⚠ CUDA-12 libs: prepend /opt/conda/lib/python3.13/site-packages/nvidia/*/lib to LD_LIBRARY_PATH
  for GPU steps (see memory cupy-cuda12-libpath).

## NEXT (Merlin's order — execute, don't re-ask)
1. B011 @40Hz done → B926 @40Hz densify. 2. 3-ch stacks `--fs 40`. 3. reversed-template noise-rate
control. 4. trust battery. 5. B011↔PGC coincidence. 6. dv/v `--origin-anchor`.

## 3-COMPONENT go/no-go RESOLVED (Merlin-guided, decisive)
My "horizontals fail from misalignment" diagnosis was WRONG — Merlin: S is simultaneous across a
sensor's 3 comps, a constant lag is translation-invariant to stacking; per-day lag search is FATAL
(1 sample/25ms -> ~1.25% fake dv/v via S-anchor). Horizontals fail from PURITY, not alignment.
Ran the zero-GPU purity-stratified H test on B926 (110 certified fams + PNSN episode days):
- Z 97/110 pass (control OK); H1 DEAD (caus 0.77 anti-causal, 0 pass); H2 23/110 pass.
- H2 PROVEN REAL not artifact: base-rate 21% cert vs 2% non-cert (gate informative); split-half
  within 0.96 / cross 0.00 (gap 0.96 ~ Z's 0.98 = family-specific coda); not narrowband (Guard C);
  H2-vs-Z coda cc 0.35 = independent of Z (not bleed); dv/v r(Z,H2)=0.25.
- Asymmetry (H1 dead): azimuth span 185deg (radiation pattern doesn't explain), H1/H2 noise floor
  1.31 (H1 not just noisier). H1 DOES carry family-specific energy (within 0.91) but ANTI-CAUSALLY
  timed -> standard 2-4s window misses it. H1 = real signal in wrong window, future shifted-window job.
POLICY (Merlin): fleet Z-only is the science payload; stacker writes H stacks FREE so H-gate is a
free CPU by-product/station; each station EARNS H2; H2 = noisy independent consistency check, NOT a
new axis; inversion doesn't need it. NO 3x GPU (rejected that framing).
Deliverables: B926 gated 3-comp dv/v (Z 110 std0.22%, H2 23 std0.59%); scripts certify_fwd_vs_rev.py,
finalize_3comp_dvv.py; B011 auto-finalizing; 5-station batch (B001/B004/B003/B927/B928) on rollout agent.
